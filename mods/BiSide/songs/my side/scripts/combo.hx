function postCreate() {
    // Hide the combo counter and judgments
    comboGroup.visible = false;

    // Hide the health bar and associated HUD elements
    healthBarBG.visible = false;
    healthBar.visible = false;
    iconP1.visible = false;
    iconP2.visible = false;

  }
