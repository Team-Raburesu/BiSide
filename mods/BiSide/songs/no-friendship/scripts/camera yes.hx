
function postUpdate() {

        if (curCameraTarget == 1) {
            camFollow.setPosition(670, 705);
        }
        if (curCameraTarget == 0) {
            camFollow.setPosition(400, 705); 
        }
    }
